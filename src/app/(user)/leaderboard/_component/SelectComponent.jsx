import * as React from "react";

import {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectLabel,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import {
  categories,
  certificates,
  contributeType,
  eventTypes,
  locations,
} from "@/utils/data";
export function SelectComponent({ operator }) {
  
  
}
